name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        go: ["1.26.x", "1.27.x"]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ matrix.go }}
          cache: true
      - name: Check formatting
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "$unformatted"
            exit 1
          fi
      - run: go test -race -timeout 5m ./...
      - run: go vet ./...
      - run: go build -trimpath ./cmd/turnwire
      - name: Exercise built CLI and MCP endpoints
        run: python3 -I scripts/smoke.py ./turnwire
      - if: runner.os == 'Linux'
        run: GOOS=windows GOARCH=amd64 go build -trimpath ./cmd/turnwire

  macos-release:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-15-intel, macos-15]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.26.x"
          cache: true
      - name: Verify macOS release compatibility
        run: |
          set -euo pipefail
          python3 -IB -m unittest discover -s scripts -p 'test_*.py'
          source scripts/macos-release-env.sh
          go build -trimpath -ldflags "-s -w" -o turnwire ./cmd/turnwire
          python3 -I scripts/check_macos_target.py ./turnwire
          python3 -I scripts/smoke.py ./turnwire
